"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.serve = void 0;
const url_1 = require("url");
const serve = function (config) {
    const ctx = this;
    if (typeof config === 'number') {
        const url = ctx.store.get('server.dev.url');
        url.port = `${config}`;
        ctx.store.set('server.dev.url', url);
        return ctx;
    }
    if (typeof config === 'string') {
        ctx.store.set('server.dev.url', new url_1.URL(config));
        return ctx;
    }
    if (config instanceof url_1.URL) {
        ctx.store.set('server.dev.url', config);
        return ctx;
    }
    ctx.store.set('server.dev', config);
    return ctx;
};
exports.serve = serve;
