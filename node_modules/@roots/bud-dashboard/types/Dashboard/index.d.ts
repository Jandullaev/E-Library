import { Dashboard as Contract, Framework } from '@roots/bud-framework';
import { Service } from '@roots/bud-framework';
import { MultiCompiler } from 'webpack';
/**
 * Dashboard service
 *
 * @public
 */
export declare class Dashboard extends Service implements Contract {
    /**
     * @public
     * @decorator `@bind`
     * @decorator `@once`
     */
    bootstrap(): Promise<void>;
    /**
     * Run the dashboard
     *
     * @public
     * @decorator `@bind`
     * @decorator `@once`
     */
    run(compiler?: MultiCompiler): Promise<Framework>;
}
//# sourceMappingURL=index.d.ts.map