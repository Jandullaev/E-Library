import { Framework } from '@roots/bud-framework';
import { StatsCompilation } from 'webpack';
/**
 * Event compiler done hook
 *
 * @remarks
 * Generates and emits `hmr.json` with proxy/dev server information
 * for use in Acorn.
 *
 * @param stats - webpack stats object
 * @returns stats
 *
 * @public
 */
export default function (this: Framework, stats: StatsCompilation): Promise<StatsCompilation>;
//# sourceMappingURL=event.compiler.stats.d.ts.map