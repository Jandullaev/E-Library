import { Signale } from '@roots/bud-support';
import { Bud } from '../../Bud';
import * as CLI from '../cli.interface';
/**
 * @internal
 */
export declare class Runner {
    cli: CLI.Options;
    /**
     * @internal
     */
    app: Bud;
    /**
     * @internal
     */
    logger: Signale;
    /**
     * Class constructor
     *
     * @param cli - CLI state
     * @param options - Bud options
     * @internal
     */
    constructor(cli: CLI.Options);
    /**
     * Initialize bud application
     *
     * @internal
     * @decorator `@bind`
     */
    initialize(): Promise<Bud>;
    /**
     * Main process
     *
     * @param build - Boolean value indicating if compilation should occur
     *
     * @internal
     * @decorator `@bind`
     */
    make(): Promise<Bud>;
}
//# sourceMappingURL=index.d.ts.map