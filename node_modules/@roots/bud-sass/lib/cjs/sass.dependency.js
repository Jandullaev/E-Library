"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveLoader = exports.importSassImplementation = void 0;
/**
 * Imports sass/sass implementation
 *
 * @param app -- Bud instance
 * @returns dart-sass
 *
 * @public
 */
async function importSassImplementation(logger) {
    try {
        logger.await('attempting to import sass');
        const sass = await Promise.resolve().then(() => __importStar(require('sass')));
        logger.success('sass imported');
        return sass;
    }
    catch (e) {
        logger.error(e);
        throw new Error('sass not found. Install it with `yarn add sass --dev` or `npm i sass --save-dev`. This may be a problem with bud; please let us know what you experienced by filing an issue at https://github.com/roots/bud');
    }
}
exports.importSassImplementation = importSassImplementation;
/**
 * Resolves sass-loader
 *
 * @returns sass-loader path
 *
 * @public
 */
function resolveLoader(logger) {
    try {
        return require.resolve('sass-loader');
    }
    catch (e) {
        logger.error(e);
        throw new Error('sass-loader not found. Install it with `yarn add sass-loader --dev` or `npm i sass-loader --save-dev`. This may be a problem with bud; please let us know what you experienced by filing an issue at https://github.com/roots/bud');
    }
}
exports.resolveLoader = resolveLoader;
