"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.verify = exports.configure = void 0;
/**
 * Add postcss-scss syntax
 *
 * @internal
 */
function configure(app) {
    app.hooks.on('extension.@roots/bud-postcss.options', options => ({
        syntax: 'postcss-scss',
    }));
}
exports.configure = configure;
/**
 * Verifies that postcss-scss is installed
 *
 * @param app - Bud instance
 * @param logger - Extension logger
 *
 * @internal
 */
function verify(app, logger) {
    const options = app.hooks.filter('extension.@roots/bud-postcss.options');
    if (options?.syntax == 'postcss-scss') {
        logger.success('postcss configured to handle scss syntax');
    }
    else {
        logger.warn('There was a problem adding postcss-scss to the build. Consider filing an issue at https://github.com/roots/bud. You may be fine as long as you are not using postcss syntax in your scss stylesheets.');
    }
}
exports.verify = verify;
